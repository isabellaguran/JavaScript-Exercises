//Multiply 5 with 3, and alert the result:
alert(5 *3);

//Divide 20 by 10, and alert the result:
alert(20 /10);

//Alert the remainder when 50 is divided by 5.
alert(50 % 5);

//Use the correct assignment operator that will result in x being 15 (same as x = x + y).
x = 10;
y = 5;
x +=y;

//Use the correct assignment operator that will result in x being 50 (same as x = x * y).
x = 10;
y = 5;
x *= y;
